# cmake-easy
# A minimal cmake file.

cmake_minimum_required(VERSION 3.20)
project(simple)
add_executable(simple main.cpp) # This says make the target 'simple'
                                # using 'main.cpp'
