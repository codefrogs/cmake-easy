README.txt

To run the script type:
cmake --log-context -P messages.cmake

If in a rush, on Linux, you can type:
./start.sh

But it's usually best to practice typing out the options. It's slower,
but you'll remember them better that way.
